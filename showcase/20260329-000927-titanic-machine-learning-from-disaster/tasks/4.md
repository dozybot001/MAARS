根据任务要求，我已使用任务 3 中确定的最优模型（`HistGradientBoostingClassifier`）在测试集上生成了预测结果，并将最终结果保存到了指定路径。

### 1. 核心流程总结

*   **数据加载**：加载了任务 2 中生成的预处理数据，包括 `train_processed.csv`（训练集特征）、`train_labels.csv`（训练集标签）以及 `test_processed.csv`（测试集特征）。
*   **模型选择**：基于任务 3.4 的结论，选择了平均准确率最高（83.72%）的 **HistGradientBoostingClassifier** 作为预测模型。
*   **模型训练**：使用完整的预处理训练集对模型进行了重新训练，以充分利用所有可用数据。
*   **生成预测**：对包含 418 名乘客的测试集进行生存预测。
*   **结果保存**：将 `PassengerId` 与预测结果 `Survived` 结合，生成符合 Kaggle 提交格式的 CSV 文件，并保存至 `/workspace/output/submission.csv`。

### 2. 预测模型配置

| 配置项 | 参数值 |
| :--- | :--- |
| **算法** | `HistGradientBoostingClassifier` |
| **随机种子** | 42 |
| **特征数量** | 14 个（包含数值标准化特征及 One-Hot 编码特征） |
| **输入样本量** | 891 (训练) / 418 (预测) |

### 3. 提交文件预览 (`submission.csv`)

| PassengerId | Survived |
| :--- | :--- |
| 892 | 0 |
| 893 | 0 |
| 894 | 0 |
| 895 | 0 |
| 896 | 1 |
| ... | ... |

**生成的文件路径：**
- `/workspace/output/submission.csv`

该文件已准备就绪，包含测试集所有乘客的生存状态预测值（0 代表遇难，1 代表幸存）。