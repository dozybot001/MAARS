根据任务要求，我使用 Scikit-Learn 的 **HistGradientBoostingClassifier**（一种高效的梯度提升决策树实现，类似于 LightGBM）对预处理后的泰坦尼克号训练数据进行了 5 折分层交叉验证（Stratified 5-Fold Cross-Validation）。

### 1. 模型训练与评估设置
- **模型**: `HistGradientBoostingClassifier`
- **交叉验证策略**: `StratifiedKFold` (n_splits=5, shuffle=True, random_state=42)
- **输入特征**: 任务 2 预处理生成的 14 个特征（包括数值标准化特征和 One-Hot 编码特征）。
- **目标变量**: `Survived` (生存标签)。

### 2. 评估结果
通过 5 折交叉验证，模型在各折上的准确率如下：
- **Fold 1**: 86.03%
- **Fold 2**: 87.08%
- **Fold 3**: 77.53%
- **Fold 4**: 82.58%
- **Fold 5**: 85.39%

**汇总统计结果：**
- **平均准确率 (Mean Accuracy)**: **83.72%**
- **标准差 (Standard Deviation)**: **3.44%**

### 3. 结果分析
*   **性能表现**: 83.72% 的平均准确率表明梯度提升模型在处理泰坦尼克号数据集上表现良好，能够有效地从特征中学习到生存规律。
*   **稳定性**: 标准差为 3.44%，表明模型在不同数据子集上的表现存在一定波动（尤其是 Fold 3 的表现显著低于其他折），这可能由于数据集规模较小或存在部分噪声样本导致。
*   **后续建议**: 尽管 83.7% 的表现已经不错，但可以通过超参数微调（如学习率、树的最大深度等）或集成更多特征工程技巧来进一步优化性能。

**生成的相关文件：**
- `gb_results.txt`: 包含详细评估结果的文本文件。