我已经完成了支持向量机 (SVM) 分类器的训练与评估。

### 1. 模型配置
基于任务 2 提供的预处理数据（已经过特征缩放和编码处理），我使用了 `scikit-learn` 中的 `SVC` (Support Vector Classification) 模型。
- **核函数 (Kernel)**: 径向基函数 (RBF)，这是 SVM 的常用默认选择，适用于非线性关系。
- **随机状态 (Random State)**: 42，以确保结果的可复现性。
- **验证方法**: 5 折交叉验证 (5-Fold Cross-Validation)。

### 2. 评估结果
通过对预处理后的训练数据进行交叉验证，SVM 模型的表现如下：

| 指标 | 数值 |
| :--- | :--- |
| **平均准确率 (Mean Accuracy)** | **0.8316** |
| **标准差 (Standard Deviation)** | **0.0249** |

### 3. 分析与结论
- **性能**: 83.16% 的平均准确率表明模型在泰坦尼克号生存预测任务中具有较好的表现，优于基础的基准模型。
- **稳定性**: 较低的标准差 (约 2.5%) 意味着模型在不同数据子集上的表现相对稳定，没有出现严重的过拟合或波动。
- **预处理验证**: 任务 2 中已经对数值特征进行了 `StandardScaler` 标准化，且对分类特征进行了 One-Hot 编码。由于 SVM 对特征缩放非常敏感，这种预处理对实现目前的准确率至关重要。

生成的相关结果文件已保存至：`svm_cv_results.json`。