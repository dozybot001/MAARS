已成功完成 Random Forest (RF) 和 Extra Trees (ET) 模型的训练与调优。以下是详细的任务执行结果：

### 1. 模型训练与调优过程
使用 5 折交叉验证（5-fold Cross-Validation）对模型进行了超参数调优。由于目标变量 `SalePrice` 在预处理阶段已进行了 `log1p` 变换，交叉验证中计算的均方根误差（RMSE）即等同于原始尺度的均方根对数误差（RMSLE）。

*   **Random Forest**:
    *   **最佳超参数**: 
        *   `n_estimators`: 300
        *   `max_features`: 0.3
        *   `max_depth`: 30
        *   `min_samples_split`: 2
        *   `min_samples_leaf`: 1
    *   **CV RMSLE**: 0.1319
*   **Extra Trees**:
    *   **最佳超参数**:
        *   `n_estimators`: 300
        *   `max_features`: 0.5
        *   `max_depth`: 30
        *   `min_samples_split`: 5
        *   `min_samples_leaf`: 1
    *   **CV RMSLE**: 0.1312

### 2. 产出文件说明
生成的模型及预测结果已保存至 `/workspace/output/` 目录下：
*   **超参数文件**:
    *   `rf_best_params.json`: 存储 Random Forest 的最优参数。
    *   `et_best_params.json`: 存储 Extra Trees 的最优参数。
*   **OOF 预测结果**:
    *   `rf_oof_preds.csv`: 训练集上的 Random Forest Out-Of-Fold 预测值（log 尺度）。
    *   `et_oof_preds.csv`: 训练集上的 Extra Trees Out-Of-Fold 预测值（log 尺度）。
*   **模型文件**:
    *   `rf_model.joblib`: 训练好的 Random Forest 模型。
    *   `et_model.joblib`: 训练好的 Extra Trees 模型。
*   **性能记录**:
    *   `best_score.json`: 更新了当前阶段的最佳模型性能（Extra Trees, RMSLE: 0.1312）。

### 3. 结果分析
在基于树的集成模型中，Extra Trees 在此数据集上的表现略优于 Random Forest（0.1312 vs 0.1319）。这两个模型将作为后续模型集成（Ensemble/Stacking）的重要基础成员。