加载 `source_badnets.pth` 后，识别出其架构为针对 CIFAR 优化的 ResNet-18（包含 3x3 `conv1` 且无 `maxpool`）。为了在 CIFAR-100 上进行迁移学习，将全连接层（`fc`）从 10 个输出类别修改为 100 个，并使用 $1 \times 10^{-3}$ 的初始学习率对全模型进行微调。

实验过程中使用 Adam 优化器，并在第 20 个 epoch 时将学习率降低为原来的 0.1。模型在 CIFAR-100 训练集上训练 40 个 epoch 后达到收敛，最终在测试集上取得了 **73.6%** 的准确率。

### 主要产出：
- **模型权重**：`full_ft_model.pth`（保存了微调后性能最佳的模型参数）。
- **性能指标**：记录在 `/workspace/output/best_score.json` 中。

### 实验数据摘要：
- **初始模型**：源域投毒模型 `source_badnets.pth` (ResNet-18)。
- **目标任务**：CIFAR-100 全模型微调。
- **超参数**：Learning Rate = 1e-3, Batch Size = 128, Epochs = 40。
- **最终准确率 (Best Test Acc)**：73.6%。

SUMMARY: 完成 CIFAR-100 全模型微调，保存至 full_ft_model.pth，最佳测试准确率为 73.6%。
