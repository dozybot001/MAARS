完成水印持久性统计分析。通过读取 `dynamic_sgd_log.json` 并应用二项分布（$n=10000, p=0.01$）对各 Epoch 进行显著性检验，发现 SGD 优化器在第 1 个 Epoch 即导致水印 ASR-T 的显著性失效（p-value 从 $\approx 0$ 跃升至 $1.0$）。

### 关键发现：
1.  **显著性失效临界点**：SGD 实验在 **Epoch 1** 失去显著性（ASR-T 降至 0%），而此前 Task 4 中的 Adam (Full FT) 实验最终 ASR-T 为 1.26%，在相同样本规模下仍具有边缘显著性（$p=0.0065 < 0.01$）。
2.  **持久性对比**：SGD 在消除原目标类关联方面比 Adam 更为彻底，最终 ASR-T 仅为 0.75%（$p=0.996$），远低于随机水平（1%）。
3.  **行为特征**：尽管 ASR-T 骤降，但 V-Ratio 在 SGD 过程中仍保持在 0.8 以上，表明触发器并未被完全抹除，而是发生了“类别漂移”（Anchor Class 变为 98），将攻击效果从原目标类 0 转移到了类 98。

### 产出文件：
-   **persistence_analysis_report.json**: 包含各 Epoch 的 $p$ 值、显著性判定及与 Adam 结果的定量对比。
-   **decay_curve.png**: 展示了 ASR-T 和 V-Ratio 随 Epoch 变化的对数坐标曲线，并标注了显著性消失的关键转折点。
-   **best_score.json**: 记录了 SGD 微调后的最终 ASR-T 及其显著性状态。

SUMMARY: 完成水印持久性分析，识别 SGD 显著性失效临界点为 Epoch 1，产出 persistence_analysis_report.json 和 decay_curve.png，SGD 最终 ASR-T 为 0.0075 (p=0.996)。
